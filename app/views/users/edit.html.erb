  <div class="container px-5 px-sm-0">
    <h2 class="mt-3">プロフィール編集</h2>
    <div class="row d-flex align-items-center justify-content-center">
      <div class="row">
        <div class="mt-5 col-md-3">
          <div class="mb-5"><strong>プロフィール画像</strong></div>
          <div><%= attachment_image_tag @user, :profile_image, :fill, 200, 200, fallback: "no_image.jpg" %></div>
        </div>
        <div class="col-md-3">
        </div>
        <div class="mt-5 col-md-6">
          <div class="row mb-3">
          <h3 class="text-danger">
            <% if @user.errors.any? %>
              <%= @user.errors.count %>error
              <% @user.errors.full_messages.each do |message| %>
              <%= message %>
              <% end %>
            <% end %>
            <div><%= flash[:alert] %></div>
          </h3>
          </div>
          <%= form_with model:@user, url:"/users/#{@user.id}",local:true do |f| %>
          <div>
            <p>ニックネーム:</p>
            <p><%= f.text_field :name %></p>
          </div>
          <div>
            <p>プロフィール写真：</p>
            <p><%= f.attachment_field :profile_image %></p>
            <p>▲登録済みでも選択されてませんと表示されます</p>
          </div>
          <div>
            <p>自己紹介：</p>
            <p><%= f.text_area :introduction %></p>
          </div>
          <div>
            <p><%= f.submit "編集する", class: "mybutton btn btn-primary" %></p>
          </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
