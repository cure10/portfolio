<div class="container">
  <h3>お問い合わせ確認</h3>
  <h5>まだお問い合わせは完了しておりません！以下でよろしいですか？</h5>
  <div><%= link_to '（入力画面に戻る場合はこちらをクリックしてください）',new_contact_path %></div><br />
  <div class="row d-flex align-items-center justify-content-center">
    <div class="col-md-12 mt-4">
      <div>
        <p>ニックネーム</p>
        <p><%= @contact.name %></p>
      </div>
      <div>
        <p>返信先gmail</p>
        <p><%= @contact.email %></p>
      </div>
      <div>
        <p>電話番号</p>
        <p><%= @contact.phone_number %></p>
      </div>
      <div>
        <p>お問い合わせ内容</p>
        <p<%= @contact.message %></p>
      </div>
      <%= form_with model: @contact, url: contacts_path, local:true do |f| %>
        <%= f.hidden_field :name %>
        <%= f.hidden_field :email %>
        <%= f.hidden_field :phone_number %>
        <%= f.hidden_field :message %>
        <div><%= f.submit '送信' %></div>
      <% end %>
    </div>
  </div>
</div>